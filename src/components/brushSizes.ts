// 붓 굵기는 슬라이더로 연속 조절 (V3b: 3단계 → 연속).
// 픽셀 단위. 5세 손가락 + 캔버스 사이즈 고려해 2~64px.
export const MIN_BRUSH = 2;
export const MAX_BRUSH = 64;
export const DEFAULT_BRUSH = 16;

// 슬라이더 step (너무 미세하면 의미 없음)
export const BRUSH_STEP = 2;
